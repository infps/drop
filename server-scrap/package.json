{
  "name": "server",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "test": "bun test",
    "db:generate": "bun drizzle-kit generate",
    "db:migrate": "bun drizzle-kit migrate",
    "db:all": "bun run db:generate; bun run db:migrate",
    "db:up": "bun drizzle-kit studio",
    "db:seed": "bun run seed.ts"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2",
    "@types/bcrypt": "^6.0.0",
    "bcrypt": "^6.0.0",
    "dotenv": "^17.2.3",
    "drizzle-orm": "^0.45.1",
    "hono": "^4.11.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.8",
    "tsx": "^4.21.0"
  }
}
